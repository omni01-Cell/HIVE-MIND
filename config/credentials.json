{
    "supabase": {
        "url": "SUPABASE_URL",
        "key": "SUPABASE_KEY"
    },
    "redis": {
        "url": "REDIS_URL"
    },
    "familles_ia": {
        "openai": "${OPENAI_KEY}",
        "gemini": "${GEMINI_KEY}",
        "anthropic": "${ANTHROPIC_KEY}",
        "mistral": "${MISTRAL_KEY}",
        "kimi": "${KIMI_KEY}",
        "moonshot": "${MOONSHOT_KEY}",
        "github": "${GITHUB_KEY}",
        "groq": "${GROQ_KEY}",
        "minimax": "${MINIMAX_KEY}",
        "huggingface": "${HUGGINGFACE_KEY}",
        "nvidia": "${NVIDIA_KEY}",
        "openrouter": "${OPENROUTER_KEY}"
    }
}
